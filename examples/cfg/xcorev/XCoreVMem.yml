---
extensions:
  XCoreVMem:
    feature: XCVMem
    arch: xcvmem
    version: "1.0"
    experimental: false
    vendor: true
    # patches: []
passes:
  per_model:
    XCoreVMem:
      skip: [riscv_features, riscv_isa_info, pattern_gen]
