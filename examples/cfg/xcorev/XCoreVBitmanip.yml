---
extensions:
  XCoreVBitmanip:
    # feature: XCVbitmanip
    # arch: xcvbitmanip
    version: "1.0"
    experimental: false
    vendor: true
passes:
  per_model:
    XCoreVMac:
      overrides:
        convert_models:
          prefix: "SEAL5"
