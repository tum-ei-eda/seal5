---
models:
  XCoreVMem:
    extensions:
      XCoreVMem:
        # feature: XCVmem
        # arch: xcvmem
        version: "1.0"
        experimental: false
        vendor: true
    passes:
      overrides:
        convert_models:
          prefix: "SEAL5_"
